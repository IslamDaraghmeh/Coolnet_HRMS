/**
 * Health Module Index
 * Exports all health-related functionality
 */

// Presentation
const healthRoutes = require("./presentation/routes/health");

module.exports = {
  // Presentation
  healthRoutes,
};
